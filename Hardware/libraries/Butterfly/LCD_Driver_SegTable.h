#ifndef LCD_Driver_SegTable_h
#define LCD_Driver_SegTable_h

// This is defined in a .c file so that the compiler won't generate
// warning "only initialized variables can be placed into program memory area"
extern const           uint16_t LCD_SegTable[];

#endif